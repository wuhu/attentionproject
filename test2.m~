a = ones(5);

for i = 1:5
    for j  = 1:5
        if(i > j)
            a(i,j) = rand(1);
        end
    end
end

[r,c] = find(a < 0.5);
b = sort([r;c])

i = 1;

count = zeros(0,2);
while i < length(b)
    num = sum(b == b(i));
    count = cat(1,count,[b(i),num]);
    i = i+num;
end

for i = 